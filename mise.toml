[tools]
uv = "latest"
python = "3.14"

[settings]
python.uv_venv_auto = "create|source"

[tasks]
test = "uv run -m luna.test.runner"
check = "ty check"
lint = [
	"ruff format",
	"ruff check --fix",
]
